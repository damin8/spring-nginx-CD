language: java
jdk:
  - openjdk8

branches:
  only:
    - main
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'
script: "./gradlew clean build"

before_install:
  chmod +x gradlew

notifications:
  email:
    recipients:
      damindamin8@gmail.com
